export default interface ResourceState {
  display: boolean
  amount: number
  max: number
  rate: number
  // value: number;
  // diff: number;
  // delta: number;

  // stackable: boolean;
  // crates: number;
  // containers: number;

  // tradeable: boolean;
  // trade: number;
}
